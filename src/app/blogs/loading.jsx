import React from 'react';

const loading = () => {
    return (
        <div>
            blogs Loading page ................................................
        </div>
    );
};

export default loading;