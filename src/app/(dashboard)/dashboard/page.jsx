

const DashboardPage = () => {
    // console.log(name);  //to check if error.jsx page works specific to routes
    return (
        <div>
            Dashboard
        </div>
    );
};

export default DashboardPage;